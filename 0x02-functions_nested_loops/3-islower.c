 #include "main.h"
/**
 * _islower - function to check for lowecase character
 * @c:    is the the int that will use for the argument of the function
 * Return: 0
 */
int _islowe(int c)
{
	if (c >= 'a' && c <= 'z')
	{
		return (1);
		}
	else
		return (0)i
}
