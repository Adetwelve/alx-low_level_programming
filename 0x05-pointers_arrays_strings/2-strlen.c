#include "main.h"
#include <string.h>

/**
 *_strlen - func get length of string
 *@s: string pointer to passed to this function
 *Return: return length of string
 */
int _strlen(char *s)
{
	return (strlen(s));
}
